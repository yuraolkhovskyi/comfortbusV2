spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yuriiolkhovskiy
    username: yuriiolkhovskiy
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  cache:
    cache-names: "getActiveRidesByParams"
    caffeine:
      spec: maximumSize=2000,expireAfterWrite=600s